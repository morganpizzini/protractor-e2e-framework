'use strict';
var expect = require('chai').expect;
var index = require('../dist/index.js');

describe('dummy test for publishing', () => {
    it('should..', () => {
        expect(true).to.equal(true);
    });
});
